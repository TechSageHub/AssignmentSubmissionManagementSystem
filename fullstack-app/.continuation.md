# ASMS — Continuation Context

## Summary of Work Done

### Recent — File Preview (Inline Viewing)
- **Backend**: `GET /api/submissions/:submissionId/file` serves submission files inline (images, PDFs, text) with `Content-Disposition: inline` and proper MIME types. Auth: students see own/group files, lecturers see own assignment files.
- **Frontend**: `FilePreview` component (supports blob fetch from API and local File objects), integrated into `ViewSubmissionPage`, `GradeSubmissionPage` (lecturer sees file while grading), and `AssignmentDetailPage` (student previews before submit).
- **Added** "View" button per submission in `AssignmentSubmissionsPage` linking to `/submissions/:id`.
- **Fixed** error redirect in `ViewSubmissionPage` to go to `/assignments` instead of `/my-submissions`.

### Previous — 8 Features (all complete)
1. Admin Dashboard (role, stats, user mgmt)
2. Email Notifications (cron reminders)
3. Grade Analytics (Recharts bar chart)
4. Assignment Rubrics (criteria builder/grading)
5. Bulk Download (archiver ZIP)
6. Password Reset (forgot/reset flow)
7. Group Submissions (member picker)
8. Audit Log (activity trail)

### Infrastructure Fixes (this session)
- `db.js` — removed `pool.on('error')` listener causing infinite reconnect loop; added 3 retries with backoff (1s/2s); proper pool cleanup.
- `auth.js` — distinguish JWT errors (→401) from DB errors (→500) so transient DB drops don't log users out.

## Open Issues
- SQL Server on port 1434 (Browser service port, default is 1433) may cause instability. ECONNRESET loop fixed, but underlying port config worth investigating.

## Key Files Touched This Session
- `backend/controllers/submissionController.js` (added `getSubmissionFile`)
- `backend/routes/submissions.js` (added /:submissionId/file route)
- `backend/middleware/auth.js` (fixed 401/500 distinction)
- `backend/config/db.js` (fixed reconnect loop, retry backoff)
- `frontend/src/components/FilePreview.tsx` (new)
- `frontend/src/pages/ViewSubmissionPage.tsx` (added preview + fixed error redirect)
- `frontend/src/pages/AssignmentSubmissionsPage.tsx` (added View button)
- `frontend/src/pages/AssignmentDetailPage.tsx` (added pre-submit preview)
- `frontend/src/pages/GradeSubmissionPage.tsx` (added grading preview)

## How to Restart
- Backend: `cd fullstack-app/backend && npm start`
- Frontend: `cd fullstack-app/frontend && npm run dev`
- DB migrations already applied (all 5).
